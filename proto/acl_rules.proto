syntax = "proto3";

message AclRules {
    repeated AclRule rules = 1;
}

message AclRule {
    string src_mac = 1;
    string dst_mac = 2;
    repeated string match_rules = 3;
    Action action = 4;
}

enum Action {
    DENY = 0;
    ALLOW = 1;
}
