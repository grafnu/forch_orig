syntax = "proto3";

message AclRules {
  repeated AclRule rules = 1;
}

message AclRule {
  string eth_src = 1;
  string eth_dst = 2;
  repeated ActionRule action_rules = 3;
}

message ActionRule {
  string match_rule = 1;
  Action action = 2;

  enum Action {
    DENY = 0;
    ALLOW = 1;
  }
}
