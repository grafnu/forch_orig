syntax = "proto3";

message AuthResult {
  Identifier id = 1;
  Role role = 2;
  Segment segment = 3;

  message Identifier {
    string id = 1;
  }

  message Role {
    string role = 1;
  }

  message Segment {
    string segment = 1;
  }
}
