dist: bionic
sudo: required
language: python
python:
  - "3.7"
cach:
  pip: true
addons:
  apt:
    update: true
install:
  - bin/setup_base
script:
  - set -e
  - bin/check_style
  - bin/build_proto check
  - bin/build_docker
  - bin/setup_stack local
  - bin/test_stack
  - bin/build_proto
  - bin/package_forch
